linters:
  enable:
    - errcheck
    - staticcheck
    - govet
    - unused
    - goimports
    - misspell

linters-settings:
  goimports:
    local-prefixes: github.com/argus-5g/argus

issues:
  exclude-rules:
    - path: _test.go
      linters: [errcheck]
